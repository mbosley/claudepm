# DEPRECATED

As of claudepm v0.3.1, the full protocol has been merged directly into CLAUDE.md.

This file is kept for backwards compatibility but is no longer used.

The protocol content is now in the fenced `<!-- CLAUDEPM SECTION -->` within CLAUDE.md itself, making CLAUDE.md the single source of truth for all instructions.